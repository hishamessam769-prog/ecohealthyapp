# دليل مراجعة نسخة الاشتراكات التجريبية

هذه البيانات تجريبية ومعلّمة `is_demo=true`. لا تستخدم بيانات عملاء حقيقية أثناء الاختبار.

## خطوات الاختبار — 10 خطوات

1. ادخل بحساب CEO وافتح **الرئيسية**؛ تأكد أن مؤشرات الاشتراكات والتحصيل ليست أرقامًا ثابتة.
2. افتح **العملاء**؛ أنشئ عميلًا جديدًا وأدخل موبايل فريدًا وعنوانًا عربيًا وGPS وZone وموعد توصيل مؤكد. جرّب نفس الموبايل مرة ثانية ويجب أن يُرفض.
3. افتح **الاشتراكات**؛ راجع 32 اختيار سعر من Muscles Gain وWeight Loss، والحالات Active/Frozen/Cancelled/Completed، والأيام والرصيد المتبقي.
4. اختر اشتراكًا Active ونفّذ **Freeze** لمدة يومين؛ وافق على رسالة التأكيد ثم تأكد أن أيامًا بديلة أضيفت في نهاية الاشتراك.
5. افتح **عمليات المشتركين** على تاريخ اليوم؛ راجع Zone وGPS وموعد التوصيل والوجبات حسب Lunch/AM/PM/FullDay وملخص الإنتاج.
6. حدّد أكثر من مشترك ونفّذ **Skip اليوم**، ثم حدّد آخرين وغير عدد الحصص إلى 2، واطبع كشف الإنتاج والتوصيل.
7. حدّد أيامًا Planned واضغط **Confirm Delivered**؛ تأكد أن الحالة أصبحت `confirmed_delivered` وأن Deferred انخفض وRecognized زاد.
8. من **الاشتراكات** أنشئ Refund؛ اختر سببًا وInstaPay/Wallet وأدخل اسم ورقم المستلم. يجب أن يحسب النظام المبلغ من الأيام المتبقية تلقائيًا.
9. ادخل بحساب Finance مختلف، وافتح **مراجعة المدفوعات** ثم افتح/اطبع طلب الاسترداد، اعتمده، وسجّل مرجع إثبات الدفع. يجب أن تظهر Cash Reversal وClawback وأن يصبح الاشتراك Cancelled.
10. ادخل بحساب Sales Representative وتأكد أنه لا يرى صفحات Admin أوFinance، ثم ادخل بحساب Operations وتأكد أنه يرى كشف اليوم وأزرار التشغيل فقط حسب صلاحياته.

## الحسابات التجريبية

الحسابات لا تُنشأ داخل SQL حتى لا توضع كلمات مرور في GitHub. أنشئها بأمان بإحدى الطريقتين:

- من **المستخدمون** داخل النظام: أرسل دعوة ثم أسند الأدوار `Sales Manager` و`Sales Representative` و`Finance Accountant` و`Operations Manager` و`Customer Service`.
- أو ضع القيم `DEMO_*_EMAIL/PASSWORD` في `.env.local` محليًا ثم شغّل `pnpm seed:acceptance-users`. السكربت يطبع الإيميلات فقط ولا يطبع كلمات المرور.

للحذف: شغّل SQL `outputs/03_remove_demo_data.sql`، ثم شغّل `pnpm cleanup:acceptance-users` بعد ضبط `ALLOW_DEMO_CLEANUP=true`.
