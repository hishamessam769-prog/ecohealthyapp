# تسليم Eco Healthy ERP V13

هذا التسليم تحديث مباشر فوق V12 الموجودة حاليًا. لا تمسح قاعدة البيانات ولا تعيد تشغيل ملف الـReset.

## الملفات التي تستخدمها

1. `deliverables/eco-healthy-erp-github-ready-v13.zip`: ارفع محتواه بدل ملفات المشروع الحالية على GitHub.
2. `deliverables/SUPABASE_UPDATE_V13.sql`: شغله مرة واحدة فقط في Supabase SQL Editor.
3. `outputs/00_RESET_AND_INSTALL_COMPLETE_DATABASE.sql`: للمشروع الفارغ فقط؛ لا تستخدمه على قاعدة V12 الحالية.

## خطوات التحديث

1. افتح Supabase SQL Editor في مشروع EcoHealthy App الحالي.
2. افتح `SUPABASE_UPDATE_V13.sql` كاملًا واضغط Run مرة واحدة.
3. تأكد أن نتيجة الفحص تعرض `Installed versions: 13` وأن كل الصفوف `passed = true`.
4. فك ZIP وارفع محتواه إلى نفس مستودع GitHub بدل الملفات القديمة، من دون رفع أي ملف `.env`.
5. اترك Vercel ينفذ Deploy تلقائيًا؛ متغيرات البيئة الموجودة لا تحتاج إعادة إدخال.
6. افتح رابط النظام وسجل الدخول، ثم اختر الفرع الرئيسي من أعلى الصفحة.

## اختبار القبول السريع

1. من المبيعات أنشئ عرضًا برقم مصري مثل `01012345678`، ثم حاول إدخال نفس الرقم بصيغة `+201012345678`؛ يجب استخدام العميل نفسه وعدم إنشاء نسخة أخرى.
2. غيّر الفرع من أعلى الشاشة وانتقل لصفحة أخرى؛ يجب أن يظل الفرع المختار ثابتًا وتتغير السجلات حسبه.
3. افتح المشاريع أوالمهام؛ الأرقام يجب أن تكون محسوبة من السجلات الفعلية وليست أرقام تصميم ثابتة.
4. افتح الاشتراكات واضغط «إجراء» أمام اشتراك نشط لتجميده.
5. أنشئ Refund لاشتراك قبل مرور 6 أيام؛ يجب أن يظهر خصم `600 × عدد الأيام المؤكد تسليمها`.
6. أنشئ Refund لاشتراك بعد مرور 6 أيام؛ يجب أن يطلب من Finance إدخال الخصم وسبب القرار.
7. تأكد أن طرق الاسترداد لا تحتوي Cash وأن الموعد الأقصى الظاهر 14 يومًا.
8. افتح عمليات المشتركين وجرّب «كشف المطبخ» ثم «كشف التوصيل»؛ كل كشف يعرض الأعمدة الخاصة به فقط.
9. نفذ Confirm Delivered وتأكد أن الحالة تصبح `confirmed_delivered` ولا تبقى `delivered_pending_confirmation`.
10. راجع Dashboard وتأكد أن «تحتاج انتباهك» والأرقام تتغير من البيانات الفعلية للفرع.

## ما تم اختباره محليًا

- Production build: ناجح.
- TypeScript: ناجح.
- ESLint: ناجح.
- 29 Unit/Integration tests: ناجحة.
- Master SQL من قاعدة فارغة: ناجح.
- Reset installer فوق Legacy schema: ناجح.
- V12 → V13 incremental SQL: ناجح.
- RLS/Permission/Commercial pgTAP suites: ناجحة.
- Demo install: 824 سجلًا في 86 جدولًا، وCleanup أعاد العدد إلى صفر.

لم يتم تشغيل V13 على مشروع Supabase المستضاف أو نشره على Vercel من داخل هذه الجلسة؛ ملف التحديث والـZIP هما التسليم المطلوب لتنفذهما على حساباتك بدون مشاركة مفاتيح أوكلمات مرور.
